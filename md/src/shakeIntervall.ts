 /*

import { defineComponent } from 'vue';

// Shake the header icon

export default defineComponent({

    data(){
      return{
        input: null,
      }
    },
    computed:{
      shakeIntervall(): any{
        return this.input = document.querySelector(
            'shake',
          ) as HTMLInputElement;
      }
     }
  })
  */